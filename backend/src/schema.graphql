# import * from './generated/prisma.graphql'
#The above line actually is doing something! Currently how Prisma imports types into a graphql file

type Mutation {
    createItem(title: String, description: String, price: Int, image: String, largeImage: String): Item!  
}

type Query {
    items: [Item]!
}